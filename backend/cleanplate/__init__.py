"""CLEANPLATE — self-hostable media restoration suite.

One shared engine behind eight tools and a pipeline builder. Every tool posts
to the same job service; progress streams over WebSocket; one GPU job runs at a
time; originals are never written to.
"""

__version__ = "1.0.0"
