# Loaded by `vite build --mode desktop` (the Tauri build). The packaged app
# loads from tauri://localhost, so it must call the engine by absolute URL.
# The web build uses the default mode with an empty base (same-origin + nginx).
VITE_API_BASE=http://localhost:8000
